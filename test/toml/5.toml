# 这是一条注释

# 这个表有一个字符串字段和一个整数字段
[table1]
string_field = "Hello, world!"  # 字符串字段
int_field = 42  # 整数字段

# 这是一条空白行

# 这个数组有三个元素
[arr1]

# 第一个元素是一个字符串
"apple"

# 第二个元素是一个整数
2

# 第三个元素是一个表
[arr1.table1]
name = "Alice"
age = 30